/**
 * @description a basic object with two fields: Number and Color
 */
class Car {
    constructor(NUMBER, COLOR) {
        this.NUMBER = NUMBER;
        this.COLOR = COLOR;
    }
}

module.exports = Car;